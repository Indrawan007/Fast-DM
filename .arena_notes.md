
## Batch 10 — v2.7.0 B2.1 (pushed f3ade45, awaiting user cargo test)
- NEW src/downloader/aria2_rpc.rs (~560ln): JSON-RPC 2.0 via reqwest (NO json feature → manual to_vec/text+parse), daemon supervisor static DAEMON Mutex<Option<Child>> const_new, kill_on_drop, probe=getVersion w/ token (auto-detects foreign daemon via secret mismatch), wait_ready 6s.
- download(): guard → ensure_daemon → changeGlobalOption live limit (uses promote_config RAW speed in mod.rs branch!) → addUri pause-true → unpause (ignore err) → 600ms loop: user status first (Paused→forcePause+return, Cancelled→forceRemove+removeDownloadResult+return partial kept), tellStatus keys const, patch_from_status pure, complete→removeDownloadResult, filename from files[0] only if aria2::is_generic_filename.
- config.rs: rpc_port u16 default 6800 (serde default fn default_rpc_port) + Config::rpc_secret()/rpc_secret_in(dir) (uuid simple[..16], 0600, param-dir for tests) + 2 tests.
- aria2.rs widened: pub(crate) CHROME_UA, pub(crate) is_generic_filename.
- window.rs: normalize_url_input magnet passthrough; wants_quality_dialog early-false magnet; +2 tests.
- mod.rs: pub mod aria2_rpc; spawn_supervised branch `is_yt → magnet → is_direct → universal` (magnet cfg.max_overall_speed := promote_config raw).
- Tests now: 118+10+2 = expect 130; +config test already in 118? (was 118 → added 2 cfg + 2 window + 8 rpc = +12 → 130).
- Checker lesson: python balance scanners MUST strip strings BEFORE comments (`//` inside url strings caused 3 false unclosed-paren reports; fixed via mini-lexer state machine; aria2.rs ( +1 is pre-existing lexer artifact both HEAD and now).
- User tree note: their local uncommitted youtube.rs fmt-noise from earlier may STILL exist → instruct `git restore .` before `git pull` (remote 706f5b4→f3ade45 ff clean).
- Remaining: B2.2 (migrasi http per-proses → daemon; deferred, low risk), D8 tray, CI clippy.
